# Feast Feature Store config
project: my_project

provider: local

# Online store (Redis active)
online_store:
    type: redis
    connection_string: "localhost:6379"

# Online store (PostgreSQL commented out)
# online_store:
#     type: postgres
#     host: localhost
#     port: 5432
#     database: feast
#     user: postgres
#     password: postgres

# Offline store
offline_store:
    type: file

# Registry
registry: data/registry.db
